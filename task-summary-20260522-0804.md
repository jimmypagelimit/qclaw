# Task Summary: Album Cover Download Failure Analysis

**Time**: 2026-05-22 08:02
**Status**: Blocked

## Issue

The async `download-covers.js` script failed with `NativeCommandError` when trying to read the database file.

## Root Cause

1. **Hardcoded path**: Both `download-covers.ts` and `database.ts` have hardcoded database path `G:/原创计划/music`
2. **Drive not mounted**: G: drive is a network mount that's not available in the exec context
3. **UNC path inaccessible**: The UNC path `\\10.0.2.4\qemu\原创计划\music\music` is also not accessible from exec context

## Evidence

```
Exec completed (warm-sho, code 1)
'G:\原创计划\music':String) [], RemoteException + FullyQualifiedErrorId : NativeCommandError
at Object.openSync (node:fs:562:18)
at Object.readFileSync (node:fs:446:35)
```

## Available Drives

- C: (NTFS, ~282GB free)
- E: (empty, 0 bytes)
- **G: and H: NOT MOUNTED**

## Compilation Status

- TypeScript compilation succeeded (swift-me, code 0)
- Output: `dist/cli.js`, `dist/download-covers.js`, `dist/server.js`

## Recommended Solutions

1. **Modify scripts** to accept database path via:
   - Environment variable (e.g., `ALBUM_DB_PATH`)
   - Command-line argument (e.g., `--db-path`)
   
2. **Create local database copy** in the album-tracker directory

3. **Mount network drive** in exec context (requires user action)

## Files Affected

- `src/download-covers.ts` - Line 16: `const DB_PATH = 'G:/原创计划/music'`
- `src/db/database.ts` - Line 11: `const DEFAULT_DB_PATH = 'G:/原创计划/music'`

## Next Steps

Wait for user to either:
- Mount G: drive / fix network share permissions
- Approve script modification to make path configurable
- Provide local database copy location
