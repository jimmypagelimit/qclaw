# 2026-06-08 每日工作总结 (17:02)

## Git 状态
- **Commit**: b24a05e
- **Push**: 成功
- **修改文件**: 11个已跟踪文件
- **新增文件**: 173个（含脚本、封面、文档等）

## 主要工作

### 1. 数据库修复：listen_history album_id 映射错误 ✅
**根因**：年度表（albums_2024/2025/2026）的自增ID和albums总表的ID完全不对应。旧版数据迁移直接把年度表ID写入listen_history的album_id字段。

**严重程度**：2026年140条中124条错，2024/2025影响较小。

**修复过程**：
1. 审计：运行 `_audit_mapping.py` 确认不匹配程度
2. 重建：从年度表以(album_name, artist)为唯一键，重新映射到albums表的正确album_id
3. 重建记录：共1028条（2024:418, 2025:394, 2026:216）
4. 补数据：8条年度表中专辑在albums表不存在，手动插入7条
5. 备份：`_music_latest.db.bak.before_rebuild`
6. 导出：更新 `database.sql`，Git commit + push

### 2. 海朋森专辑封面补全与修正 ✅ (两次迭代)

**第一次（11:35-11:42）**：
- 网易云找到两张专辑封面（She Came Back From the Square / 我不要别的历史）
- 下载封面到 `public/covers/`
- 更新数据库 `cover_image_url` 字段
- 发现并修正专辑名错误：错误"我不要你死于一事无成" → 正确"我不要别的历史"
- Git推送两次（b33699a: 添加封面；e56e8c4: 修正专辑名）

**第二次（11:46-11:48）**：
- 用户反馈"我不要别的历史"依然没有封面
- 根因：修正了专辑名后，封面文件名未同步更新
- 修复：重命名封面文件 + 更新数据库路径 + Git推送（7f534a6）

**教训**：修改专辑名后必须同时检查封面文件名和数据库路径是否一致

### 3. 仪表盘简化 ✅ (commit b7419da)
- 删除：风格分布、国家分布、收听趋势、年度收听排行 四个图表卡片
- 保留：总库统计卡片 + 专辑总榜 Top 10 + 艺人总榜 Top 10
- 前端：重写 `loadDashboard()`，删除图表渲染函数
- 新增：`loadAlbumLeaderboard()`、`loadArtistLeaderboard()` 两个榜单渲染函数
- 新增 `/api/artists` 后端接口（按收听次数聚合艺人）

### 4. 艺人头像下载 ✅ (commit ce4ca9a)
- `artists` 表新增 `image_url` 字段（TEXT）
- 编写批量下载脚本 `_download_artist_images.py`
  - 主力源：**Deezer Artist API**（国外艺人几乎全覆盖）
  - 补充源：**iTunes Album Artwork**（中文艺人Deezer没的）
- **307/311 艺人下载成功**（98.7% 覆盖率）
- 4个失败：太冷门（尸体派对、刘润洁等）
- 后端 `server.ts` 改 `/api/artists` 从artists表直读 `image_url`
- 前端 `app.js` 适配 `image_url` 字段
- Git推送（含307张头像图片）

### 5. 年份筛选BUG修复 ✅ (两次迭代)

**问题**：
- 用户问"保罗麦卡特尼的新专辑又丢了？"
- Paul McCartney - The Boys of Dungeon Lane (ID=540) 在数据库中存在
- 但Web界面年份筛选看不到
- 根因：`/api/albums?year=2026` 用的是 `JOIN listen_history`，只返回有收听记录的专辑

**第一次修复（失败）**：
- 改成 `release_year`（LEFT JOIN）
- 全部返回但不包含海朋森等非2026年发行的专辑

**第二次修复（失败）**：
- 用户指出海朋森等专辑搜不到了
- 改回 `listen_year` + INNER JOIN + GROUP BY
- 同时修复 GROUP BY 缺失导致的 SQL 500 错误

**最终方案（成功）**：
- `listen_year` + `INNER JOIN (listen_history GROUP BY album_id)` + `GROUP BY a.album_id`
- 2026年可搜到142张收听专辑（含海朋森、Paul McCartney等非2026年发行但2026年收听的）

**关键教训**：年份筛选按**收听年份**而非发行年份，因为用户找的是"某年听过什么"

### 6. 文档更新 ✅ (12:13)
**MEMORY.md** 更新3处：
- 专辑入库同步规则：双表同步 → 单表模式（年度表已废弃）
- 听歌记录数据库：统计规则改为JOIN，数据库现状更新为 albums 517条 | listen_history 1028条 | artists 313条
- 听歌记录维护流程：双表同步 → 单表模式

**ALBUM_TRACKER_RULES.md** 完全重写（10.5KB）：
- 标准操作流程更新为10步（单表模式）
- 数据库架构部分重写（废除albums_2024/2025/2026年度表）
- 新增「错误6：listen_history album_id 映射错误」
- 新增工具脚本 `_audit_mapping.py` ✅ 和 `_rebuild_listen_history.py` ✅ 状态标记
- Web查询逻辑：server.ts 4个API端点 + 前端 index.html/app.js 同步更新

### 7. 与老表核对收听记录 ✅ (12:40-12:53)
编写核对脚本 `_verify_old_vs_new.py`，对比年度表 `total_listen_count` 与 `listen_history` 实际计数：

**2024年**：243条，9条不符，0条缺失
- 所有不符都是「老表=0，listen_history=1」
- 根因：老表数据质量问题（听了1次却记成0），当前listen_history是对的，无需修改

**2025年**：192条，0条不符，0条缺失 ✅

**2026年**：140条，3条不符，2条缺失 ⚠️

用户确认修复规则：
1. 2026年不符的3条中，1次的不需要改 → `春子 Haruko`（None→1）和 `Remains`（0→1）保持现状
2. 缺失的要改 → 补入 `listen_history`
3. `Teen of Denial (Joes Story)` 和 `Teens of Denial` 是两张不同的专辑 → 不是重复条目

## 用户批评 ⚠️

**时间**：16:02

**用户原话**："我发现你犯的错误可太多了，这是最重要的项目，你可得长点心"

**三个错误**：
1. **没想清楚就动手** → 第一次改 `release_year`，没问用户想搜"发行年"还是"收听年"
2. **改了不验证** → 每次改完没本地跑API直接重启服务，连续崩了两次
3. **GROUP BY 两次都忘加** → 说明写SQL时脑子里没形成稳定结构

**根因**：数据结构经历多次变化（年度表→单表→流派中间表），我脑子里没有当前状态的稳定地图就开始改代码。

**承诺的改进流程**（album-tracker核心查询改动）：
1. 先想清楚需求（发行 vs 收听）
2. 写出完整SQL逻辑
3. 本地Python先跑通
4. 再改server.ts + 编译 + 重启 + 验证

## 待办事项

### 远程路径（待H盘挂载）
- 远程路径可达时，复制 `_music_latest.db` 到 `\\10.0.2.4\qemu\原创计划\music\music`
- 远程路径可达时，复制 `database.sql` 到 `\\10.0.2.4\qemu\原创计划\music\database.sql`

### 2026年收听记录修复（待用户确认）
- 删除 `Teens of Denial` 多出的2条 `listen_history`（从4条删到2条）
- 补入缺失的2条：
  - `我不要别的历史 No Need for Another History - 海朋森 [Hiperson]` → 先查album_id（应该是id=446）
  - `Teen of Denial (Joes Story) - Car Seat Headrest` → 总表可能没有，需要先新增专辑再补listen_history

## 技术细节

### 数据库架构变更
- **废除**：albums_2024 / albums_2025 / albums_2026 年度表
- **保留**：albums（总表）、listen_history（收听记录）、artists（艺人）、genres（流派）、styles（风格）
- **映射规则**：以 `(album_name, artist)` 为唯一键，从年度表重新映射到albums表的正确album_id

### 修复脚本（可删除）
- `_debug_mapping.py` - 初步诊断
- `_audit_mapping.py` - 全面审计 ✅
- `_rebuild_check.py` - 重建前检查缺失专辑
- `_find_missing.py` - 查找缺失专辑
- `_rebuild_listen_history.py` - 核心重建脚本 ✅
- `_fix_remaining.py` - 修复剩余缺失
- `_fix_final.py` - 最终修复 + 填充
- `_export_db.py` - 导出 database.sql
- `_git_fix.sh` - Git提交
- `_verify_api.py` - 验证API
- `_check_2025.py` - 验证2025年数据
- `_verify_old_vs_new.py` - 核对老表vs新表 ✅
- `_verify_old_vs_new2.py` / `_verify_old_vs_new3.py` - 迭代版本

### Web服务状态
- **服务器**：PID 5864，端口3456
- **接口测试**：`/api/stats`、`/api/albums`、`/api/artists` 三个接口测试通过
- **艺人封面**：有封面显示 `<img>`，无封面显示 🎵 占位符

## 输出质量反思

### 自查问题（09:31）
- ✅ 无过程堆砌 - 技术排查记录有必要
- ✅ 无信息轰炸 - 使用表格/代码块/emoji组织
- ⚠️ 记忆文件有重复内容（昨日文件写入了3次相同内容）
- ✅ 无过度解读 - 用户问什么答什么
- ✅ 无废话 - 直接执行，结果导向

### 改进措施
- 今天写记忆文件时先检查是否已存在相同内容
- 将RYM CloakBrowser方案封装为可复用脚本

## 飞书通知

⚠️ **heartbeat上下文message工具400错误** - 无法直接从heartbeat发送飞书通知，结果写入此artifact文件。

**总结内容**：
```
2026-06-08 每日工作总结

✅ 数据库修复：listen_history album_id 映射错误（重建1028条记录）
✅ 海朋森专辑封面补全与修正（两次迭代）
✅ 仪表盘简化（删除4个图表，保留榜单）
✅ 艺人头像下载（307/311，98.7%覆盖率）
✅ 年份筛选BUG修复（listen_year + INNER JOIN）
✅ 文档更新（ALBUM_TRACKER_RULES.md完全重写）

⚠️ 用户批评：album-tracker核心查询改动犯了三错（没想清楚就动手、改了不验证、GROUP BY两次都忘加）

📊 Git commit: b24a05e | Push: 成功
```
