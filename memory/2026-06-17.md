

## A项目重构（16:00-16:15）
### 专辑详情页重构完成

**目标**：展示曲目信息 + 卫星表信息

**修改文件**：
1. `server.ts`：专辑详情 API 返回 tracks、external_ratings、review_url
2. `app.js`：新增 renderTracks()、renderExternalRatings() 函数
3. `style.css`：新增曲目列表和外部评分样式
4. `.gitignore`：移除 dist/ 忽略（需提交编译后的 server.js）

**测试结果**：
- API 返回 tracks 和 external_ratings 正常
- 服务重启成功（PID 6116，端口 3456）

**Git**：
- Commit: 82be746 `A项目：专辑详情页重构 - 展示曲目列表+外部评分+乐评链接`
- Push: 成功（main -> main）

**已知问题**：
- 中文乱码（控制台 GBK 编码，不影响浏览器）
- 曲目信息不完整（仅部分专辑有 tracks 数据）
- 外部评分覆盖率低（24.8%）

---

## L项目推进（14:28-15:30）
### 网易云中文歌词批量获取 — Phase 2 首轮完成

**结果：35 → 75 张专辑有歌词（+40张）**
- 分3批处理（3 + 10 + 20张），共444首歌词
- 命中率 ~75-80%，部分专辑不在网易云（海朋森、Tizzy Bac、葬尸湖部分）
- 11首带翻译+双语对齐

**技术方案**
- 专辑API `/api/album/{id}` 被 -462 反爬阻挡 → 退化为歌曲搜索+专辑名过滤
- 搜索精度80%，偶有专辑错配（苏紫旭错配live合辑）和重复（法兹22首重复）
- 每首间隔1.5s，避免触发频率限制
- 脚本：`tasks/lyrics-expert/wy_batch.py`

**已知问题**
- 海朋森 - 成长小说：网易云10首全部NO LYRIC
- 葬尸湖 - 冬霾：仅2首无歌词
- Tizzy Bac 部分专辑不在网易云
- 搜索精度导致错配

**剩余：447张（中文246 + 英文201）**
- 中文可继续跑管道，英文需另建LRCLIB管道
# 2026-06-17

## 网易云 opencli 浏览器对接成功
- 已完成网易云音乐登录（账号：城醒庙墨麒麟）
- 16:00 用 `opencli browser netease` 完成搜索、导航、state 查看等操作
- 优势：绕过 API 反爬 -462 限制，能看到完整的专辑详情、封面、曲目、歌词（含翻译）
- 局限：专辑页在 iframe 里渲染，extract 拿不到 iframe 内容

## 数据库曲目表缺失
- 检查 `_music_latest.db` 表结构：`albums` + `listen_history`，**没有 tracks/songs 表**
- 当前曲目只存在于 L 项目的 `.lrc` 文件和 MusicBrainz tracklist 文件中
- 用户对此感兴趣，后续可能新增 `tracks` 表

## 小知识
- [disc:1] 表示第一张 CD
- 网易云弹窗交互有延迟
