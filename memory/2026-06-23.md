## V项目（视频制作工具箱）创建
- **位置**：`tasks/v-project/`
- **核心脚本**：`_gen_batch_bg.py`（CD风格背景图生成，参数敲定版）
- **已有资产**：20张封面（9a94a49）+ 20张背景图（ff669b1），保留在 `video_thumbs/` 下不动
- **README**：含CD风格参数表、项目定位、待开发清单
- **待开发**：视频合成（背景图+音乐+文字->MP4）

## V项目 - 封面位置调整
- **调整内容**：COVER_X 从 80px -> 180px（封面右移100px）
- **触发**：用户反馈"图片偏右一些，左侧留大空间"
- **结果**：生成测试图 `tasks/v-project/output/test_v2_left180.png`

## V项目 - 封面参数定稿
- **定稿参数**（2026-06-23 确认）：
  - COVER_X = 180px（左留白 180px）
  - COVER_SIZE = 550px（封面加大）
- **TOOLS.md / README / 脚本docstring** 全部同步更新
- **测试图**：`tasks/v-project/output/test_v3_left180_size550.png`

### 13:42  -  Web显示修复

- A项目V封面批量生成了20张
- Git提交完成（7bf8b77），push可能超时需重试
- A项目首页艺人排名显示为0，需排查API返回是否含album_count
## V项目（视频制作工具箱）创建
- **位置**：`tasks/v-project/`
- **核心脚本**：`_gen_batch_bg.py`（CD风格背景图生成，参数敲定版）
- **已有资产**：20张封面（9a94a49）+ 20张背景图（ff669b1），保留在 `video_thumbs/` 下不动
- **README**：含CD风格参数表、项目定位、待开发清单
- **待开发**：视频合成（背景图+音乐+文字->MP4）

## V项目 - 封面位置调整
- **调整内容**：COVER_X 从 80px -> 180px（封面右移100px）
- **触发**：用户反馈"图片偏右一些，左侧留大空间"
- **结果**：生成测试图 `tasks/v-project/output/test_v2_left180.png`

## V项目 - 封面参数定稿
- **定稿参数**（2026-06-23 确认）：
  - COVER_X = 180px（左留白 180px）
  - COVER_SIZE = 550px（封面加大）
- **TOOLS.md / README / 脚本docstring** 全部同步更新
- **测试图**：`tasks/v-project/output/test_v3_left180_size550.png`

### 13:42 - Web显示修复

- A项目V封面批量生成了20张
- Git提交完成（7bf8b77），push可能超时需重试
- A项目首页艺人排名显示为0，需排查API返回是否含album_count

### 15:16 - 艺人排行榜"0次"Bug修复

- **问题**：首页艺人排行榜显示"0 次"，但API返回的listen_count有正确值（65, 44, 38等）
- **根因**：前端 `renderArtistLeaderboard` 用了 `ar.total_listen_count`，但 API `/api/artists` 返回的字段名是 `listen_count`
- **修复**：`ar.total_listen_count` → `ar.listen_count`（在 `public/js/app.js` 的 `renderArtistLeaderboard` 函数中）
- **状态**：已修复，刷新首页即可看到正确数字
# 2026-06-23

## 封面文件名修复

### 问题
前端加载封面 404，发现封面文件名含单引号 `'` 在浏览器 URL 中需要编码，前端直接拼接路径导致 404。

### 修复
- 批量重命名 covers/ 目录下所有含单引号的文件（移除单引号）
- 同步更新数据库 `cover_image_url` 字段
- 共修复 12 个文件

### 后续规则
- 新专辑入库时封面文件名应避免特殊字符（单引号、括号等），只用字母数字和短横线
- Web 服务修改后已重启

## 木马封面错误

### 问题
木马（Muma）专辑封面显示错误（可能是错误封面或被下载成无关图片）。

### 修复
- 从网易云搜索 ID 36195，下载同名专辑「木马 - 木马Muma」的正确封面
- 文件名改为 `439-Muma-Muma.jpg`（去除所有特殊字符）
- 删除旧封面文件，更新数据库路径
- Web 服务已重启

### 发现
covers/ 目录下 240 个文件含特殊字符（空格、括号、方括号等），目前已满 524 张专辑，未来可考虑整体清理。
# 2026-06-23

## 封面文件名修复

### 问题
前端加载封面 404，发现封面文件名含单引号 `'` 在浏览器 URL 中需要编码，前端直接拼接路径导致 404。

### 修复
- 批量重命名 covers/ 目录下所有含单引号的文件（移除单引号）
- 同步更新数据库 `cover_image_url` 字段
- 共修复 12 个文件

### 后续规则
- 新专辑入库时封面文件名应避免特殊字符（单引号、括号等），只用字母数字和短横线
- Web 服务修改后已重启

## 木马封面错误

### 问题
木马（Muma）专辑封面显示错误（可能是错误封面或被下载成无关图片）。

### 修复
- 从网易云搜索 ID 36195，下载同名专辑「木马 - 木马Muma」的正确封面
- 文件名改为 `439-Muma-Muma.jpg`（去除所有特殊字符）
- 删除旧封面文件，更新数据库路径
- Web 服务已重启

### 发现
covers/ 目录下 240 个文件含特殊字符（空格、括号、方括号等），目前已满 524 张专辑，未来可考虑整体清理。

## Pluto 封面错误

### 问题
鄭宜農（Enno Cheng）的 Pluto 专辑封面显示错误。

### 修复
- 从网易云搜索，找到正确专辑「PLUTO - 郑宜农」（ID: 35377225）
- 下载正确封面：`https://p1.music.126.net/cXYIjwVlVn70Zglt2JA0Kw==/109951165609150760.jpg`
- 文件名改为 `441-Enno-Cheng-Pluto.jpg`（去除方括号和特殊字符）
- 删除旧封面文件（`441-鄭宜農 [Enno Cheng]-Pluto.jpg`）
- 更新数据库 `cover_image_url` 为 `/covers/441-Enno-Cheng-Pluto.jpg`
- Web 服务已重启

### 经验
- 艺人名含方括号 `[]` 会导致文件名异常，入库时必须清理
- 建议封面文件名格式：`{id}-{Artist简写}-{Album简写}.jpg`，只保留 ASCII 字母、数字、短横线
# 2026-06-23

## V项目（视频制作工具箱）创建
- **位置**：`tasks/v-project/`
- **核心脚本**：`_gen_batch_bg.py`（CD风格背景图生成，参数敲定版）
- **已有资产**：20张封面（9a94a49）+ 20张背景图（ff669b1），保留在 `video_thumbs/` 下不动
- **README**：含CD风格参数表、项目定位、待开发清单
- **待开发**：视频合成（背景图+音乐+文字->MP4）

## V项目 - 封面位置调整
- **调整内容**：COVER_X 从 80px -> 180px（封面右移100px）
- **触发**：用户反馈"图片偏右一些，左侧留大空间"
- **结果**：生成测试图 `tasks/v-project/output/test_v2_left180.png`

## V项目 - 封面参数定稿
- **定稿参数**（2026-06-23 确认）：
  - COVER_X = 180px（左留白 180px）
  - COVER_SIZE = 550px（封面加大）
- **TOOLS.md / README / 脚本docstring** 全部同步更新
- **测试图**：`tasks/v-project/output/test_v3_left180_size550.png`

### 13:42 - Web显示修复
- A项目V封面批量生成了20张
- Git提交完成（7bf8b77），push可能超时需重试
- A项目首页艺人排名显示为0，需排查API返回是否含album_count

### 15:16 - 艺人排行榜"0次"Bug修复
- **问题**：首页艺人排行榜显示"0 次"，但API返回的listen_count有正确值（65, 44, 38等）
- **根因**：前端 `renderArtistLeaderboard` 用了 `ar.total_listen_count`，但 API `/api/artists` 返回的字段名是 `listen_count`
- **修复**：`ar.total_listen_count` → `ar.listen_count`（在 `public/js/app.js` 的 `renderArtistLeaderboard` 函数中）
- **状态**：已修复，刷新首页即可看到正确数字

### 15:41 - 三张专辑封面修复
- **木马 Muma (ID=439)**：文件存在且正确 ✅
- **Itinerary Jo's Moving Day (ID=433)**：之前已修复，正常 ✅
- **Pluto 鄭宜農 (ID=441)**：封面文件不存在，从网易云重新下载（108KB）→ `441-Enno-Cheng-Pluto.jpg`
- Web服务重启后全部验证通过
## V项目（视频制作工具箱）创建
- **位置**：`tasks/v-project/`
- **核心脚本**：`_gen_batch_bg.py`（CD风格背景图生成，参数敲定版）
- **已有资产**：20张封面（9a94a49）+ 20张背景图（ff669b1），保留在 `video_thumbs/` 下不动
- **README**：含CD风格参数表、项目定位、待开发清单

### 12:13 - Fires in the Distance《Circadian Promise》入库
- **专辑ID**: 595
- **艺人**: Fires in the Distance
- **流派**: Melodic Death Metal
- **发行年**: 2025
- **MBID**: 3e2c0d8a-1d6f-4b53-a4f5-7c1eb0bf9c89（已验证）
- **来源**: 用户指定入库
- **操作**: 停止Web服务 → 写入albums表(含description) + listen_history → 下载封面(Cover Art Archive) → 复制到public/covers/ → 更新cover_image_url → 导出SQL → 重启Web服务
- **Git**: commit `a1e4c93` (album-tracker仓库)

### 12:30 - V项目背景图定稿v2
- **参数敲定**: COVER_X=180, COVER_SIZE=550（左侧大封面，右侧留文字区）
- **批量生成**: 20张v2背景图，输出到 `video_thumbs/20th_century_indie_bg_v2/`
- **Git**: commit `7bf8b77` (workspace仓库)，push成功

### 13:35 - A项目首页艺人排名"0次"Bug修复
- **问题**: 首页艺人排行榜显示"0 次"
- **根因**: 前端用`ar.total_listen_count`，但API返回字段名是`listen_count`
- **修复**: `public/js/app.js`中`renderArtistLeaderboard`函数改为`ar.listen_count`

### 14:00 - Itinerary封面单引号路径Bug修复
- **问题**: album_id=433 (Jo's Moving Day - Itinerary) 封面404
- **根因**: 文件名含单引号，Python路径处理转义错误
- **修复**: 批量重命名12个含单引号的封面文件（去掉单引号），数据库同步更新，Web服务重启
- **影响文件**: 433, 179, 183, 199, 231, 249, 472, 48, 488, 493, 497, 506

### 15:22~15:43 - 三张专辑封面修复最终状态
- **木马 Muma (ID=439)**: 从网易云下载正确封面 → `439-Muma-Muma.jpg` ✅
- **Itinerary (ID=433)**: 从网易云重新下载 → `433-Jos-Moving-Day-Itinerary.jpg` ✅  
- **Pluto 鄭宜農 (ID=441)**: 从网易云重新下载(108KB) → `441-Enno-Cheng-Pluto.jpg` ✅
- 三张文件内容均已验证正确（读取图片确认）
- Web服务已重启(PID 7164)
- **用户反馈**: 木马显示正确；Itinerary和Pluto用户看到空白→建议Ctrl+Shift+R强制刷新（可能是浏览器缓存）

### 16:41 - 木马封面Web服务重启
- **问题**: 用户反馈木马封面显示空白
- **排查**: 文件内容正确(51KB)，数据库路径正确(`/covers/439-Muma-Muma.jpg`)
- **根因**: Web服务缓存
- **解决**: 重启Web服务(PID 7164→新PID)，服务状态200 OK
- **临时脚本**: `_tmp_fix_muma.py`、`_tmp_restart.py` 已清理

### 16:46  -  封面排查

- A项目木马专辑封面显示为空，Web服务已重启但问题未解决
- 需用Playwright或HTTP直接检查封面文件是否可访问

### 16:41~16:46 - 木马封面修复（第二次）
- **第一次（16:41）**: 重启Web服务，但未解决
- **根因**: sql.js 是内存数据库，启动时加载一次DB文件到内存，之后修改文件不会自动同步
- **第二次修复**: 
  1. 查了HTTP API返回值 → 仍是旧乱码路径 → 确认是内存缓存问题
  2. 彻底：kill所有node进程 → 释放端口 → 重新启动
  3. 验证API返回 `"/covers/439-Muma-Muma.jpg"` ✅
- **教训**: sql.js项目改DB后必须kill进程+释放端口+重启，仅restart不够
- 临时脚本 `_tmp_fix_muma.py`, `_tmp_check.py`, `_tmp_restart.py` 已存在于workspace，待清理

### ~17:00 - 今日工作总结（本周二）
**完成事项**：
1. **封面修复**：木马、Itinerary、Pluto三张专辑封面
2. **Itinerary单引号Bug**：批量重命名12个含单引号的封面文件
3. **A项目首页Bug**：艺人排行榜"0次"问题修复
4. **V项目背景图v2**：20张背景图定稿生成

### 18:53 - All Things Must Pass 封面修复
- album_id=456（George Harrison - All Things Must Pass）
- 封面文件不存在（被之前错误的封面覆盖）
- 从网易云重新下载：picUrl → 保存为 `456-George Harrison-All Things Must Pass.jpg`
- 大小 312KB，HTTP 访问正常 ✅
