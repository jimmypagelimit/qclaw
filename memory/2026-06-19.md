
## 匿名旅行者数据提取方法总结（09:31）

### 核心流程（已验证成功）
1. **Playwright启动浏览器** → `playwright.chromium.launch(headless=False)`
2. **visit首页** → 获取session/cookie（必需，API直调返回空）
3. **键盘输入搜索词** → `page.type('input[placeholder]', query, delay=150)`
4. **监听XHR响应** → `page.on('response', lambda r: responses.append(r) if 'quick_search' in r.url else None)`
5. **提取JSON** → `response.json()` 中获取搜索结果
6. **获取详情** → `page.evaluate('fetch("/api/meta/album/{at_id}")')`

### 关键规则
- **短词(<4字)搜不到** → 必须用完整专辑名
- **曲目标题格式** `"1.曲名"` → 需去数字前缀提取歌名
- **无时长数据** → duration字段缺失，置NULL
- **郭佳短评** → rocknuts[].content字段
- **评分** → rating_anon字段，满分100

### 数据库写入
- 描述：风格 + 郭佳短评 + 外部ID
- 曲目：source='anontraveler'
- 评分：写入 external_ratings 表（score_scale='100'）

## 收听记录更新（09:06-09:31）

- 陈楚生《荒芜之境》(id=559) +1次，总2次（2026-06-19）
- Feeble Little Horse《Bitknot》(id=553) +1次，总2次（2026-06-19）
- 注意：`total_listen_count` 字段已删除，收听次数完全从 `listen_history` 实时计算

## 匿名旅行者数据提取方法总结（09:31）

### 核心流程（已验证成功）
1. **Playwright启动浏览器** → `playwright.chromium.launch(headless=False)`
2. **visit首页** → 获取session/cookie（必需，API直调返回空）
3. **键盘输入搜索词** → `page.type('input[placeholder]', query, delay=150)`
4. **监听XHR响应** → `page.on('response', lambda r: responses.append(r) if 'quick_search' in r.url else None)`
5. **提取JSON** → `response.json()` 中获取搜索结果
6. **获取详情** → `page.evaluate('fetch("/api/meta/album/{at_id}")')`

### 关键规则
- **短词(<4字)搜不到** → 必须用完整专辑名
- **曲目标题格式** `"1.曲名"` → 需去数字前缀提取歌名
- **无时长数据** → duration字段缺失，置NULL
- **郭佳短评** → rocknuts[].content字段
- **评分** → rating_anon字段，满分100

### 数据库写入
- 描述：风格 + 郭佳短评 + 外部ID
- 曲目：source='anontraveler'
- 评分：写入 external_ratings 表（score_scale='100'）

## 歌词显示功能设计（~10:00）

用户要求在 Web 页面上显示歌词。已提出方案：

**歌词目录**：`tasks/lyrics-expert/lyrics/{Artist}/{Album}/`（.txt + .lrc）
**显示位置**：专辑详情弹窗，曲目列表每行可展开
**方案要点**：
- 每首歌 accordion 展开/折叠，📜图标表示有歌词
- 衬线字体 Cormorant Garamond，cream色底，像歌词本内页
- 后端新增 `GET /api/albums/:id/lyrics`，按 artist+album_name 匹配歌词目录
- 前端异步加载，无歌词静默不占空间
- 待用户确认：accordion vs 整块显示、是否显示时间戳、是否开始编码

## 歌词显示功能已完成（~10:32）

### 实现内容
1. **后端** — `server.ts` 新增 `GET /api/albums/:id/lyrics`
   - 按 artist + album_name 匹配 `tasks/lyrics-expert/lyrics/` 目录
   - 读取 .txt 和 .lrc 文件
   - 归一化匹配：去非字母数字字符后精确匹配，失败则前15字符前缀匹配

2. **前端** — `app.js` + `style.css`
   - 专辑详情弹窗曲目列表每行加 📜 标记
   - 曲目列表下方独立大块歌词区（用户选择了整块显示而非 accordion）
   - LRC 时间戳绿色斜体标记
   - Cormorant Garamond 衬线字体 + cream 底色 + 可滚动(max-height 600px)

### 数据
- **匹配率**：238/517 张专辑（46%）有歌词
- 英文歌词主要来自 LRCLIB，中文来自网易云
- Web 服务运行在 http://localhost:3456

### 已知问题
- 中文专辑匹配率较低（目录名/文件名编码差异）
- `Car Seat Headrest Twin Fantasy` 扁平目录导致重复条目
- 短文件名前缀匹配可能误匹配

### artifact 文件
- `tasks/lyrics-display-feature_2026-06-19.md`
