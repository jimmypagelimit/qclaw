# 数据库去重修复报告

**时间**: 2026-06-06 21:20  
**触发**: 用户发现 Twin Fantasy 显示两份，tc 计数错误

---

## 问题根因

1. **迁移时未去重**: 之前的数据库迁移（v1→v3）没有做 albums 表去重
2. **tc=0 不可能存在**: 用户纠正——只要在数据库里的专辑至少听过1次，tc=0 100% 是重复条目
3. **Twin Fantasy 计数错误**: `albums_2026` 旧表 id=56（tc=3）未迁移到 `listen_history`

---

## 执行操作

### 1. 停止 Web 服务
- `taskkill /F /IM node.exe` → PID 4392 已终止

### 2. 修复 Twin Fantasy（album_id=323）
- `listen_history` 添加 3 条 2026 年记录（2026-03-05/15/25）
- `albums.total_listen_count` 更新为 13（2025年10次 + 2026年3次）
- 删除 `albums_2026` 表中重复条目 id=56

### 3. 删除 tc=0 重复条目
- 删除 `albums` 表中 `total_listen_count=0` 的 125 条记录
- 同步清理 `listen_history` 中对应的孤儿记录

### 4. 验证结果
| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| albums 表 | 509 条 | **384 条** |
| listen_history | 1020 条 | **1023 条** |
| Twin Fantasy tc | 10 | **13** |

### 5. 导出 + 同步
- 数据库复制回 UNC 路径 `\\10.0.2.4\qemu\原创计划\music`
- 导出 `database.sql`
- Git commit + push（commit `9581075`）

### 6. 清理 + 重启
- 删除 8 个临时 `_*.py` 脚本
- Web 服务重启成功（PID 4020，http://localhost:3456）

---

## 结论

✅ 数据一致性已恢复  
✅ 重复条目已清理（125 条）  
✅ Twin Fantasy 计数已修正（tc=13）  
✅ Git 已推送，数据库已同步  

---

## 遗留问题

- `albums_2024/2025/2026` 旧年度表仍保留（用户要求不删）
- 这些旧表中可能还有重复数据，但不影响新结构查询

---

**修复脚本**: `_fix_db_v5.py`（已删除）  
**Git commit**: `9581075`  
**数据库状态**: ✅ 正常  
