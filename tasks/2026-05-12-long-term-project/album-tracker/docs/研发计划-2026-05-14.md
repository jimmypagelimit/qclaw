# album-tracker 研发计划 — 2026-05-14（周四）

> 基于 2026-05-13 项目状态，优先级排序

---

## 🔴 P0：收尾遗留（暂缓）
- 封面最后 2 张 — 暂缓
- 封面同步验证 — 暂缓

---

## 🟡 P1：CLI 工具修复

### 3. CLI `listen` 命令 bug
- **问题：** `node dist/cli.js listen` 显示 null/NaN，ID 查找逻辑有误
- **临时方案：** 用 Web API (curl PUT /api/albums/:id/listen) 更可靠
- **修复方向：** 检查 `search` 函数的 ID 返回逻辑，确保正确映射年份表和总表

**预计时间：** 30 分钟

### 4. CLI `add` 命令 ID 返回不准确
- **问题：** `CLI add` 返回的 ID 与实际写入不符（总表 vs 年份表 ID 独立）
- **修复方向：** 返回时标注是哪个表的 ID，或返回两张表的 ID

**预计时间：** 20 分钟

---

## 🟢 P2：Web UI 增强

### 5. 仪表盘年度对比图表
- 当前 HTML 中有 `#year-compare-chart` 容器，但 `loadYearCompareChart()` 函数尚未实现
- **内容：** 各年份收听次数对比柱状图（数据已有：`data.yearListens`）
- **预计时间：** 15 分钟

### 6. 专辑库排序优化
- 当前排序在前端 JS 完成（client-side sort），数据量大时性能差
- **优化方向：** 将排序参数传给后端 API，由 SQLite 处理
- **预计时间：** 30 分钟

### 7. 专辑详情弹窗增强
- 收听历史时间线（如果有数据）
- 相似专辑推荐（基于风格/国家/评分）
- **预计时间：** 45 分钟

---

## 🔵 P3：新功能（暂缓）
- 收听记录导入、数据导出、CLI 交互模式 — 暂缓

---

## 📅 今日执行顺序（P1 → P2）

| 顺序 | 任务 | 预计时间 |
|------|------|----------|
| 1 | CLI listen bug 修复 | 30 min |
| 2 | CLI add ID 返回修复 | 20 min |
| 3 | 年度对比图表 | 15 min |
| 4 | 专辑库排序后端化 | 30 min |
| 5 | 午休 |
| 6 | 专辑详情弹窗增强 | 45 min |

**今日小计：** ~2.5 小时（不含午休）

---

## 📝 备注

- Web UI 现在只有两个页面：仪表盘 + 专辑库（排行榜已移除）
- 维护操作全走 CLI（`node dist/cli.js`）
- 封面覆盖率：2026 97%、2025 100%、2024 98%
- 数据库：albums 495条 | albums_2026 128条 | albums_2025 192条 | albums_2024 243条
- Web 服务器需重启才能反映数据库变更（sql.js 内存模型）
